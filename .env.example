NODE_ENV=production

PGUSER=neo_fmc_user
PGPASSWORD=change_this_strong_password
PGDATABASE=neo_fmc_db

DATABASE_URL=postgresql://neo_fmc_user:change_this_strong_password@postgres:5432/neo_fmc_db

JWT_SECRET=generate_with__node_-e_"console.log(require('crypto').randomBytes(64).toString('hex'))"

ALLOWED_ORIGINS=http://your-domain.com,https://your-domain.com

OPENAI_API_KEY=

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

WEBHOOK_SECRET=generate_with__node_-e_"console.log(require('crypto').randomBytes(32).toString('hex'))"
